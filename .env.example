# Local development/data-tooling only. Never expose this value through Vite client variables or commit it.
# Phase 13 production access uses the server-side Worker secret instead:
#   cd worker && npx wrangler secret put JOSHUA_PROJECT_API_KEY
JOSHUA_PROJECT_API_KEY=
