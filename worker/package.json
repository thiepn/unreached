{
  "name": "unreached-private-continuity-worker",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "types": "wrangler types --config wrangler.generated.jsonc",
    "check": "npm run types && tsc --noEmit",
    "deploy": "wrangler deploy --config wrangler.generated.jsonc"
  },
  "dependencies": {
    "jose": "6.2.10"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "wrangler": "4.125.0"
  }
}
