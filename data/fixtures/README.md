# U2 synthetic fixtures

These records are deliberately synthetic and exist only to exercise schemas, adapters, chunking, provenance, and cross-reference validation.

- They are **not** real mission statistics.
- They must never be presented as real people-group information.
- `XZZ` is used as a user-assigned synthetic country code and `qaa` as a local-use language code.
- Production datasets must come through an approved source entry and the same validation pipeline.
